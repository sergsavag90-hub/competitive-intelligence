# 🚀 Розгортання Competitive Intelligence в Proxmox

Повна документація з розгортання Selenium Grid + Competitive Intelligence Tool у Proxmox середовищі з підтримкою контейнеризації та автоматичного масштабування.

## 📋 Зміст

- [Вимоги](#вимоги)
- [Швидкий старт](#швидкий-старт)
- [Детальне розгортання](#детальне-розгортання)
- [Масштабування](#масштабування)
- [Моніторинг](#моніторинг)
- [Backup та відновлення](#backup-та-відновлення)
- [Усунення проблем](#усунення-проблем)

---

## 🎯 Архітектура

```
┌─────────────────────────────────────────────────────────────┐
│                    Proxmox VE Host                          │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              LXC Container (Ubuntu 22.04)            │  │
│  │                                                      │  │
│  │  ┌─────────────┐  ┌──────────────┐  ┌───────────┐  │  │
│  │  │ Selenium    │  │ Chrome Node  │  │ Firefox   │  │  │
│  │  │ Hub         │  │ x N          │  │ Node x N  │  │  │
│  │  │ :4444       │  │              │  │           │  │  │
│  │  └─────────────┘  └──────────────┘  └───────────┘  │  │
│  │                                                      │  │
│  │  ┌─────────────┐  ┌──────────────┐  ┌───────────┐  │  │
│  │  │ Edge Node   │  │ Intelligence │  │ Ollama    │  │  │
│  │  │ x N         │  │ Web :5000    │  │ LLM       │  │  │
│  │  │             │  │              │  │ :11434    │  │  │
│  │  └─────────────┘  └──────────────┘  └───────────┘  │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Особливості архітектури:

- **Кожен браузер у окремому контейнері** - легке масштабування
- **Docker-in-LXC** - використання Docker всередині LXC контейнера
- **Динамічне масштабування** - додавання/видалення браузерних нод на льоту
- **VNC доступ** - віддалений перегляд браузерів через VNC
- **Автоматичний restart** - всі контейнери автоматично перезапускаються
- **Resource limits** - обмеження ресурсів для кожного контейнера

---

## 💻 Вимоги

### Апаратні вимоги

**Мінімальна конфігурація:**
- CPU: 4 cores
- RAM: 8 GB
- Storage: 50 GB
- Network: 1 Gbps

**Рекомендована конфігурація:**
- CPU: 8-16 cores
- RAM: 16-32 GB
- Storage: 100 GB SSD
- Network: 1 Gbps

**Розрахунок ресурсів:**
- Selenium Hub: ~512MB RAM, 0.5 CPU
- Кожна Chrome нода: ~2GB RAM, 2 CPU
- Кожна Firefox нода: ~1.5GB RAM, 1.5 CPU
- Кожна Edge нода: ~2GB RAM, 2 CPU
- Intelligence Web: ~1GB RAM, 1 CPU
- Ollama (опціонально): ~4-8GB RAM, 2-4 CPU

### Програмні вимоги

- Proxmox VE 7.0+ або 8.0+
- Ubuntu 22.04 LTS template
- Docker 24.0+
- Docker Compose v2.20+

---

## 🚀 Швидкий старт

### Варіант 1: Автоматичне розгортання (найпростіше)

```bash
# 1. На Proxmox хості створіть LXC контейнер
cd /tmp
git clone <your-repo-url>
cd competitive-intelligence/proxmox
./create-lxc.sh 100 selenium-grid dhcp

# 2. Під'єднайтеся до контейнера
pct enter 100

# 3. Завантажте проект
cd /opt/competitive-intelligence
git clone <your-repo-url> .

# 4. Запустіть deployment
./deploy.sh
```

**Готово!** Через 5-10 хвилин всі сервіси будуть доступні.

### Варіант 2: Ручне розгортання

```bash
# 1. Створіть LXC контейнер вручну через Proxmox Web UI
# 2. Встановіть Docker
# 3. Завантажте проект
# 4. Запустіть deploy.sh
```

---

## 📦 Детальне розгортання

### Крок 1: Підготовка Proxmox хоста

```bash
# Оновіть систему
apt update && apt upgrade -y

# Завантажте Ubuntu template (якщо немає)
pveam update
pveam download local ubuntu-22.04-standard_22.04-1_amd64.tar.zst
```

### Крок 2: Створення LXC контейнера

#### Автоматично:

```bash
cd /path/to/project/proxmox
./create-lxc.sh 100 competitive-intel 192.168.1.100 192.168.1.1
```

#### Вручну через Web UI:

1. Відкрийте Proxmox Web Interface
2. Виберіть ноду -> Create CT
3. Налаштуйте:
   - **General:**
     - Hostname: `competitive-intelligence`
     - Password: `<secure-password>`
     - Unprivileged: `No` (для Docker підтримки)
   
   - **Template:**
     - Storage: `local`
     - Template: `ubuntu-22.04-standard`
   
   - **Root Disk:**
     - Storage: `local-lvm`
     - Disk size: `50 GB`
   
   - **CPU:**
     - Cores: `4`
   
   - **Memory:**
     - Memory: `8192 MB`
     - Swap: `4096 MB`
   
   - **Network:**
     - Bridge: `vmbr0`
     - IPv4: `DHCP` або static IP
     - IPv6: `SLAAC` або відключено
   
   - **DNS:**
     - DNS domain: `local`
     - DNS servers: `8.8.8.8 8.8.4.4`

4. **ВАЖЛИВО!** Після створення, увімкніть nesting:
   ```bash
   pct set 100 --features keyctl=1,nesting=1
   ```

### Крок 3: Налаштування контейнера

```bash
# Під'єднайтеся до контейнера
pct enter 100

# Оновіть систему
apt update && apt upgrade -y

# Встановіть необхідні пакети
apt install -y curl wget git ca-certificates gnupg lsb-release sudo vim htop
```

### Крок 4: Встановлення Docker

```bash
# Додайте Docker GPG key
mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Додайте Docker repository
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null

# Встановіть Docker
apt update
apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Перевірте установку
docker --version
docker compose version
```

### Крок 5: Завантаження проекту

```bash
# Створіть директорію проекту
mkdir -p /opt/competitive-intelligence
cd /opt/competitive-intelligence

# Завантажте з Git
git clone <your-repository-url> .

# Або завантажте архів
# На Proxmox хості:
pct push 100 /path/to/project.tar.gz /tmp/project.tar.gz
# В контейнері:
tar -xzf /tmp/project.tar.gz -C /opt/competitive-intelligence
```

### Крок 6: Конфігурація

```bash
# Перегляньте та відредагуйте config.yaml
vim config.yaml

# Основні параметри для налаштування:
# - targets: URL конкурентів
# - selenium_grid.hub_url: залиште як є для Docker
# - ollama.enabled: true/false (вимагає додаткових ресурсів)
```

### Крок 7: Запуск

```bash
# Запустіть автоматичний deployment
./deploy.sh

# Скрипт виконає:
# ✓ Перевірку системи
# ✓ Завантаження Docker образів
# ✓ Створення необхідних директорій
# ✓ Запуск всіх сервісів
# ✓ Health checks
# ✓ Відображення інформації
```

### Крок 8: Перевірка

```bash
# Перевірте статус
./deploy.sh --status

# Перевірте логи
./deploy.sh --logs

# Відкрийте в браузері
# http://<container-ip>:4444  - Selenium Grid Console
# http://<container-ip>:5000  - Intelligence Web Dashboard
```

---

## 🔧 Масштабування

### Динамічне масштабування браузерів

Проект включає скрипт `scale-browsers.sh` для легкого масштабування:

```bash
# Показати поточний статус
./scale-browsers.sh --status

# Масштабувати Chrome до 5 нод
./scale-browsers.sh chrome 5

# Масштабувати Firefox до 3 нод
./scale-browsers.sh firefox 3

# Масштабувати Edge до 2 нод
./scale-browsers.sh edge 2

# Показати метрики
./scale-browsers.sh --metrics

# Очистити мертві контейнери
./scale-browsers.sh --cleanup
```

### Приклади масштабування

**Сценарій 1: Легке навантаження**
```bash
./scale-browsers.sh chrome 2
./scale-browsers.sh firefox 1
./scale-browsers.sh edge 1
# Total: 4 ноди, ~8GB RAM
```

**Сценарій 2: Середнє навантаження**
```bash
./scale-browsers.sh chrome 5
./scale-browsers.sh firefox 3
./scale-browsers.sh edge 2
# Total: 10 нод, ~20GB RAM
```

**Сценарій 3: Високе навантаження**
```bash
./scale-browsers.sh chrome 10
./scale-browsers.sh firefox 5
./scale-browsers.sh edge 3
# Total: 18 нод, ~36GB RAM
```

### Обмеження ресурсів

```bash
# Перегляньте використання ресурсів
docker stats

# Якщо потрібно обмежити окремий контейнер
docker update --cpus 2 --memory 2g selenium-chrome-1
```

---

## 📊 Моніторинг

### Вбудовані інструменти

```bash
# Показати статус всіх сервісів
./deploy.sh --status

# Показати метрики системи
./deploy.sh --metrics

# Показати логи всіх сервісів
./deploy.sh --logs

# Показати логи конкретного сервісу
./deploy.sh --logs intelligence-web
./deploy.sh --logs selenium-hub

# Health check
./deploy.sh --health
```

### Selenium Grid Console

Відкрийте в браузері: `http://<container-ip>:4444/ui`

Тут ви побачите:
- Активні сесії
- Доступні ноди
- Черга завдань
- Статус кожної ноди

### Web Dashboard

Відкрийте в браузері: `http://<container-ip>:5000`

Features:
- Перегляд зібраних даних
- Порівняння конкурентів
- Аналіз трендів
- Експорт даних

### VNC доступ до браузерів

Кожна браузерна нода має VNC сервер:

```
Chrome Node 1:  http://<container-ip>:7901
Chrome Node 2:  http://<container-ip>:7902
Firefox Node:   http://<container-ip>:7903
Edge Node:      http://<container-ip>:7904
```

### Prometheus (опціонально)

Для production середовища рекомендується налаштувати Prometheus + Grafana.

---

## 💾 Backup та відновлення

### Автоматичний backup

```bash
# Створити backup
./deploy.sh --backup

# Backups зберігаються в:
# /opt/competitive-intelligence/backups/backup_YYYYMMDD_HHMMSS.tar.gz
```

### Що включено в backup:

- База даних SQLite
- Конфігураційні файли
- .env файл
- Експортовані дані

### Відновлення з backup

```bash
# Зупиніть сервіси
./deploy.sh --stop

# Відновіть дані
cd /opt/competitive-intelligence
tar -xzf backups/backup_20241216_120000.tar.gz

# Запустіть сервіси
./deploy.sh --start
```

### Proxmox Backup

Рекомендується також використовувати вбудовані можливості Proxmox:

```bash
# Створити snapshot контейнера
pct snapshot 100 backup-before-update

# Створити backup контейнера
vzdump 100 --mode snapshot --storage local

# Відновити з backup
pct restore 100 /var/lib/vz/dump/vzdump-lxc-100-*.tar.zst
```

---

## 🔍 Усунення проблем

### Проблема: Контейнери не запускаються

```bash
# Перевірте логи
./deploy.sh --logs

# Перевірте чи запущений Docker
systemctl status docker

# Перевірте nesting
pct config 100 | grep features

# Має бути: features: keyctl=1,nesting=1
```

### Проблема: Selenium Hub не відповідає

```bash
# Перевірте чи запущений Hub
docker ps | grep selenium-hub

# Перегляньте логи Hub
docker logs selenium-hub

# Перезапустіть Hub
docker restart selenium-hub

# Перевірте здоров'я
curl http://localhost:4444/wd/hub/status
```

### Проблема: Браузерні ноди не з'єднуються

```bash
# Перевірте мережу
docker network inspect selenium-grid

# Перевірте чи ноди бачать Hub
docker exec selenium-chrome-1 ping selenium-hub

# Перегляньте логи ноди
docker logs selenium-chrome-1

# Перезапустіть ноду
docker restart selenium-chrome-1
```

### Проблема: Недостатньо пам'яті

```bash
# Перевірте використання пам'яті
free -h
docker stats --no-stream

# Зменште кількість нод
./scale-browsers.sh chrome 2
./scale-browsers.sh firefox 1

# Або збільште RAM контейнера
pct set 100 --memory 16384
```

### Проблема: Повільна робота

```bash
# Перевірте CPU
htop

# Перевірте I/O
iotop

# Перевірте мережу
iftop

# Оптимізуйте ресурси
# 1. Зменште кількість нод
# 2. Вимкніть Ollama якщо не використовується
# 3. Очистіть логи: rm -rf logs/*
# 4. Очистіть старі дані: rm -rf data/old/*
```

### Проблема: Docker daemon не запускається

```bash
# Перевірте чи ввімкнено nesting
pct config 100 | grep nesting

# Якщо nesting=0, увімкніть:
pct set 100 --features keyctl=1,nesting=1

# Перезапустіть контейнер
pct reboot 100

# Перевірте Docker
systemctl status docker
journalctl -u docker -f
```

---

## 🛠️ Додаткові команди

### Керування сервісами

```bash
# Запустити
./deploy.sh --start

# Зупинити
./deploy.sh --stop

# Перезапустити
./deploy.sh --restart

# Оновити
./deploy.sh --update

# Повне видалення
./deploy.sh --cleanup
```

### Робота з контейнерами

```bash
# Список контейнерів
docker ps

# Логи контейнера
docker logs -f <container-name>

# Під'єднатися до контейнера
docker exec -it <container-name> bash

# Статистика контейнерів
docker stats
```

### Налаштування firewall (на Proxmox хості)

```bash
# Відкрийте порти для контейнера
iptables -A FORWARD -p tcp -d <container-ip> --dport 4444 -j ACCEPT
iptables -A FORWARD -p tcp -d <container-ip> --dport 5000 -j ACCEPT
iptables -A FORWARD -p tcp -d <container-ip> --dport 7901:7904 -j ACCEPT

# Збережіть правила
iptables-save > /etc/iptables/rules.v4
```

---

## 📚 Корисні посилання

- [Selenium Grid Documentation](https://www.selenium.dev/documentation/grid/)
- [Proxmox VE Documentation](https://pve.proxmox.com/wiki/Main_Page)
- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)

---

## 🤝 Підтримка

Для питань та проблем:
1. Перевірте логи: `./deploy.sh --logs`
2. Запустіть health check: `./deploy.sh --health`
3. Створіть Issue у репозиторії проекту

---

## 📄 Ліцензія

MIT License - вільне використання
