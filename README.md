# 🕵️ Competitive Intelligence Tool
## Інструмент конкурентної розвідки для Selenium Grid

Повнофункціональний інструмент для автоматизованого збору інформації про сайти конкурентів з використанням Selenium Grid.

## 🎯 Основні можливості

### 1. **SEO Intelligence Module**
- Збір структури ключових слів (title, meta keywords, h1-h6)
- Аналіз meta-тегів (description, robots, og:tags)
- Парсинг robots.txt та sitemap.xml
- Структурні дані (JSON-LD, Schema.org)
- Аналіз внутрішньої структури посилань

### 2. **Company Data Extractor**
- Email-адреси (всі знайдені на сайті)
- Телефони (з різними форматами)
- Фізичні адреси та локації
- Соціальні мережі (Facebook, Instagram, LinkedIn, Twitter, YouTube)
- Контактні форми та способи зв'язку

### 3. **Product/Service Catalog Scanner**
- Автоматичне виявлення товарів/послуг
- Збір цін та валют
- Категорії та підкатегорії
- Зображення товарів
- Описи та характеристики
- Наявність на складі

### 4. **Promotions Tracker**
- Акції та знижки
- Спеціальні пропозиції
- Промо-коди та купони
- Розпродажі та сезонні пропозиції
- Дати початку/кінця акцій

### 5. **Data Storage & Visualization**
- SQLite база даних для зберігання
- JSON експорт результатів
- Веб-інтерфейс для перегляду даних
- Порівняльні таблиці та графіки
- Історія змін та моніторинг

### 6. **Local LLM Analyzer (Ollama)**
- Аналіз зібраних даних
- Генерація рекомендацій
- Порівняльний аналіз конкурентів
- Виявлення трендів та можливостей
- Автоматичні звіти

## 📁 Структура проєкту

```
competitive-intelligence/
├── src/
│   ├── scrapers/
│   │   ├── seo_scraper.py           # SEO дані
│   │   ├── company_scraper.py       # Контактна інформація
│   │   ├── product_scraper.py       # Товари/послуги
│   │   └── promotion_scraper.py     # Акції та пропозиції
│   ├── database/
│   │   ├── models.py                # SQLAlchemy моделі
│   │   └── db_manager.py            # Управління БД
│   ├── analyzers/
│   │   └── ollama_analyzer.py       # LLM аналіз
│   ├── web/
│   │   ├── app.py                   # Flask веб-сервер
│   │   └── templates/               # HTML шаблони
│   └── utils/
│       ├── selenium_helper.py       # Допоміжні функції Selenium
│       └── config.py                # Конфігурація
├── tests/
│   └── test_scrapers.py             # Тести
├── docker-compose.intelligence.yml  # Docker Compose для CI tool
├── requirements.txt                 # Python залежності
├── config.yaml                      # Конфігураційний файл
└── run_intelligence.py              # Головний скрипт

```

## 🚀 Швидкий старт

### 1. Встановлення залежностей

```bash
cd competitive-intelligence
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Налаштування конфігурації

Відредагуйте `config.yaml`:

```yaml
selenium_grid:
  hub_url: "http://192.168.1.10:4444/wd/hub"
  browser: "chrome"

targets:
  - name: "Competitor 1"
    url: "https://competitor1.com"
  - name: "Competitor 2"
    url: "https://competitor2.com"

ollama:
  enabled: true
  model: "llama3.2"
  host: "http://localhost:11434"
```

### 3. Запуск збору даних

```bash
# Запуск повного аналізу всіх конкурентів
python run_intelligence.py --full

# Запуск окремих модулів
python run_intelligence.py --module seo --target "Competitor 1"
python run_intelligence.py --module products --target "Competitor 1"
```

### 4. Запуск веб-інтерфейсу

```bash
python src/web/app.py
# Відкрийте http://localhost:5000
```

## 🐳 Docker інтеграція

Додайте сервіс до існуючого Selenium Grid:

```bash
# В основній папці проєкту selenium-grid-proxmox-v3
cp competitive-intelligence/docker-compose.intelligence.yml .
docker compose -f docker-compose.yml -f docker-compose.intelligence.yml up -d
```

## 📊 Використання

### Базовий приклад

```python
from src.scrapers.seo_scraper import SEOScraper
from src.database.db_manager import DatabaseManager

# Ініціалізація
db = DatabaseManager()
scraper = SEOScraper(hub_url="http://localhost:4444/wd/hub")

# Збір даних
seo_data = scraper.scrape("https://competitor.com")

# Збереження в БД
db.save_seo_data(seo_data)
```

### Приклад з аналізом LLM

```python
from src.analyzers.ollama_analyzer import OllamaAnalyzer

analyzer = OllamaAnalyzer()
recommendations = analyzer.analyze_competitor(
    competitor_name="Competitor 1",
    data=db.get_all_data("Competitor 1")
)

print(recommendations)
```

## 🔄 Автоматичний моніторинг

Налаштуйте cron для регулярного моніторингу:

```bash
# Щоденний збір даних о 3:00
0 3 * * * /opt/selenium-grid/competitive-intelligence/venv/bin/python /opt/selenium-grid/competitive-intelligence/run_intelligence.py --full --silent
```

## 📈 Веб-інтерфейс

Веб-дашборд включає:
- 📊 Порівняльні таблиці конкурентів
- 📈 Графіки динаміки цін
- 🗺️ Карта конкурентних переваг
- 📝 Автоматичні звіти
- 🔍 Пошук по зібраним даним
- 📥 Експорт в Excel/CSV/JSON

## 🔧 Налаштування

### Додавання власних скраперів

```python
from src.utils.selenium_helper import SeleniumHelper

class CustomScraper(SeleniumHelper):
    def scrape(self, url):
        driver = self.get_driver()
        driver.get(url)
        
        # Ваша логіка парсингу
        data = {}
        
        driver.quit()
        return data
```

## 🧠 Ollama аналіз

Інструмент використовує локальну LLM для:
- Виявлення сильних/слабких сторін конкурентів
- Генерації рекомендацій для вашого бізнесу
- Аналізу цінової політики
- Виявлення трендів на ринку

## 📝 Ліцензія

MIT License - вільне використання

## 🤝 Підтримка

Для питань та пропозицій створюйте Issues в репозиторії.
