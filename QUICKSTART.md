# ⚡ Швидкий старт - 5 хвилин до запуску

Найшвидший спосіб запустити Competitive Intelligence + Selenium Grid у Proxmox.

## 📋 Передумови

- ✅ Proxmox VE 7.0+ або 8.0+
- ✅ Мінімум 8GB RAM на хості
- ✅ 50GB вільного місця

## 🚀 Крок 1: Завантажте проект

На **Proxmox хості**:

```bash
cd /tmp
git clone <your-repository-url> competitive-intelligence
cd competitive-intelligence
```

## 🏗️ Крок 2: Створіть LXC контейнер

Автоматично (рекомендовано):

```bash
cd proxmox
./create-lxc.sh 100 selenium-grid dhcp
```

Скрипт виконає:
- ✓ Створить LXC контейнер з ID 100
- ✓ Встановить Ubuntu 22.04
- ✓ Налаштує Docker
- ✓ Встановить всі залежності
- ✓ Підготує середовище

**Час виконання:** ~5-7 хвилин

## 📦 Крок 3: Завантажте код проекту

Під'єднайтеся до контейнера:

```bash
pct enter 100
```

Завантажте проект:

```bash
cd /opt/competitive-intelligence
git clone <your-repository-url> .
```

## ⚙️ Крок 4: Налаштуйте конфігурацію (опціонально)

```bash
# Відредагуйте config.yaml
vim config.yaml

# Основне що потрібно змінити:
# - targets: додайте URL конкурентів
# - ollama.enabled: встановіть false якщо не потрібен LLM аналіз
```

## 🎬 Крок 5: Запустіть!

```bash
./deploy.sh
```

Скрипт виконає:
- ✓ Перевірку системи
- ✓ Завантаження Docker образів
- ✓ Запуск Selenium Hub
- ✓ Запуск браузерних нод (Chrome x2, Firefox, Edge)
- ✓ Запуск Intelligence Web
- ✓ Запуск Ollama (якщо enabled)
- ✓ Health checks

**Час виконання:** ~3-5 хвилин

## ✅ Перевірка

Після успішного запуску, відкрийте в браузері:

**Дізнайтеся IP контейнера:**
```bash
hostname -I
```

**Доступні сервіси:**
- 🌐 Selenium Grid: `http://<ip>:4444/ui`
- 📊 Web Dashboard: `http://<ip>:5000`
- 🖥️ Chrome VNC 1: `http://<ip>:7901`
- 🖥️ Chrome VNC 2: `http://<ip>:7902`
- 🖥️ Firefox VNC: `http://<ip>:7903`
- 🖥️ Edge VNC: `http://<ip>:7904`

## 🎯 Що далі?

### Запустіть збір даних

```bash
# В контейнері
cd /opt/competitive-intelligence

# Повний аналіз всіх конкурентів
python run_intelligence.py --full

# Конкретний модуль
python run_intelligence.py --module seo --target "Competitor 1"
```

### Перегляньте результати

Відкрийте Web Dashboard: `http://<ip>:5000`

### Масштабуйте браузери

```bash
# Додайте більше Chrome нод
./scale-browsers.sh chrome 5

# Додайте Firefox ноди
./scale-browsers.sh firefox 3
```

### Моніторинг

```bash
# Статус сервісів
./deploy.sh --status

# Метрики
./deploy.sh --metrics

# Логи
./deploy.sh --logs
```

## 🆘 Щось пішло не так?

### Перевірка 1: Docker запущений?

```bash
systemctl status docker
```

Якщо ні:
```bash
systemctl start docker
```

### Перевірка 2: Nesting увімкнено?

```bash
# На Proxmox хості
pct config 100 | grep nesting

# Має бути: features: keyctl=1,nesting=1
# Якщо ні:
pct set 100 --features keyctl=1,nesting=1
pct reboot 100
```

### Перевірка 3: Контейнери запущені?

```bash
docker ps
```

Має показати ~7 контейнерів (hub + браузери + web + worker + ollama)

### Перевірка 4: Selenium Hub відповідає?

```bash
curl http://localhost:4444/wd/hub/status
```

Має повернути JSON з статусом Grid.

### Детальна діагностика

```bash
# Health check
./deploy.sh --health

# Повні логи
./deploy.sh --logs | tee debug.log
```

## 📞 Підтримка

Якщо проблема не вирішена:

1. Збережіть логи: `./deploy.sh --logs > debug.log`
2. Створіть Issue з логами
3. Або перегляньте повну документацію: [PROXMOX_DEPLOYMENT.md](./PROXMOX_DEPLOYMENT.md)

## 🎉 Готово!

Ваша система Competitive Intelligence готова до роботи!

**Наступні кроки:**
- 📝 Додайте цілі для аналізу в `config.yaml`
- 🔄 Налаштуйте автоматичний збір (cron)
- 📊 Вивчіть Web Dashboard
- 🤖 Спробуйте LLM аналіз (якщо Ollama enabled)

---

**Корисні команди:**

```bash
# Статус
./deploy.sh --status

# Restart
./deploy.sh --restart

# Backup
./deploy.sh --backup

# Масштабування
./scale-browsers.sh chrome 10

# Оновлення
./deploy.sh --update

# Зупинка
./deploy.sh --stop

# Cleanup (видалити все)
./deploy.sh --cleanup
```

---

**Приємної роботи! 🚀**
